export interface Supply {
    supplyCode: number | null,
    supplyName: string,
    qtyPerUnit: number | null,
    expireDate: string,
    unit: string,
    cost: number | null,
    buyDate: string,
    supplyType: string
}

